<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nul1.showtimenotifier" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    \
        </activity>

        <activity
            android:name=".Home_Screen"
            android:label="@string/app_name" >

        </activity>
        <activity
            android:name=".SearchResults"
            android:label="@string/title_activity_search_results" >
        </activity>
        <activity
            android:name=".SavedShows"
            android:label="@string/title_activity_saved_shows" >
        </activity>
        <activity
            android:name=".NotificationSettings"
            android:label="@string/title_activity_notification_settings" >
        </activity>
        <activity
            android:name="nul.showtimenotifier.Splash"
            android:label="@string/title_activity_splash" >
        </activity>
    </application>

</manifest>
